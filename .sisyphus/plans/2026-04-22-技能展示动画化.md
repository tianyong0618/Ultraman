# 技能展示动画化实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 点击技能按钮时，在左侧图片区域显示技能动画图片（淡入淡出），技能持续时间后恢复原图。

**Architecture:** 使用方案C（自动匹配）- 图片路径通过 `{奥特曼名}_{技能名}.jpg` 自动匹配，存放在 `public/images/skills/` 目录。技能动画状态在 useMagicBook hook 中管理，传递给 PageLeft 组件渲染。

**Tech Stack:** React, useState, CSS transition

---

## 文件结构

| 文件 | 操作 | 职责 |
|-----|------|------|
| `src/hooks/useMagicBook.js` | 修改 | 添加技能动画状态管理 |
| `src/components/PageLeft.jsx` | 修改 | 接收并渲染技能图片 |
| `src/App.jsx` | 修改 | 传递 prop |
| `public/images/skills/` | 创建 | 存放技能图片 |

---

## Task 1: 修改 useMagicBook.js 添加技能动画状态

**Files:**
- Modify: `ultraman-magic-book/src/hooks/useMagicBook.js`

- [ ] **Step 1: 添加技能动画状态**

在 `useState` 声明区域添加：
```javascript
const [activeSkill, setActiveSkill] = useState(null)      // 当前激活的技能名
const [isSkillAnimating, setIsSkillAnimating] = useState(false)  // 动画进行中
```

- [ ] **Step 2: 添加技能图片路径函数**

在 `sanitizeFilename` 函数后添加：
```javascript
// 获取技能图片路径（方案C：自动匹配）
const getSkillImage = useCallback((ultramanName, skillName) => {
  const sanitize = (name) => name.replace(/[^a-zA-Z0-9\u4e00-\u9fff]/g, '_')
  const safeUltramanName = sanitize(ultramanName)
  const safeSkillName = sanitize(skillName)
  return `/images/skills/${safeUltramanName}_${safeSkillName}.jpg`
}, [])
```

- [ ] **Step 3: 修改 playSkill 函数添加动画逻辑**

替换现有的 `playSkill` 函数：
```javascript
const playSkill = useCallback((skillIndex) => {
  if (!soundOn || !current) return
  const skillName = current.skills[skillIndex]
  if (skillName) {
    // 播放声音（保持原有逻辑）
    const safeUltramanName = sanitizeFilename(current.name)
    const safeSkillName = skillName.replace(/[^a-zA-Z0-9\u4e00-\u9fff]/g, '_')
    const audioKey = `skills/${safeUltramanName}_${safeSkillName}`
    
    if (audioPlayerRef.current) {
      audioPlayerRef.current.pause()
      audioPlayerRef.current = null
    }
    
    if (preloadedAudioRef.current[audioKey]) {
      audioPlayerRef.current = preloadedAudioRef.current[audioKey]
      audioPlayerRef.current.currentTime = 0
      const playPromise = audioPlayerRef.current.play()
      if (playPromise) playPromise.catch(() => {})
    } else {
      audioPlayerRef.current = new Audio(`/audio/skills/${safeUltramanName}_${safeSkillName}.mp3`)
      const playPromise = audioPlayerRef.current.play()
      if (playPromise) playPromise.catch(() => {})
      const skillAudio = new Audio(`/audio/skills/${safeUltramanName}_${safeSkillName}.mp3`)
      skillAudio.preload = 'auto'
      preloadedAudioRef.current[audioKey] = skillAudio
    }
    
    // 技能动画逻辑
    setActiveSkill(skillName)
    setIsSkillAnimating(true)
    
    // 技能持续时间（光线技3秒，格斗技2秒，特殊技2.5秒）
    const skillDuration = 3000  // 默认光线技3秒
    setTimeout(() => {
      setIsSkillAnimating(false)
      setActiveSkill(null)
    }, skillDuration)
  }
}, [soundOn, current, sanitizeFilename])
```

- [ ] **Step 4: 更新返回值**

在 `return` 对象中添加新状态：
```javascript
return {
  // ... existing returns
  activeSkill,
  setActiveSkill,
  isSkillAnimating,
  setIsSkillAnimating,
  getSkillImage,
}
```

- [ ] **Step 5: 运行诊断检查**

Run: `lsp_diagnostics` on `ultraman-magic-book/src/hooks/useMagicBook.js`
Expected: No errors

---

## Task 2: 修改 PageLeft.jsx 支持技能图片显示

**Files:**
- Modify: `ultraman-magic-book/src/components/PageLeft.jsx`

- [ ] **Step 1: 更新组件 props 签名**

替换函数签名：
```javascript
export const PageLeft = memo(function PageLeft({ 
  current, 
  imageError, 
  onImageError, 
  activeForm,
  activeSkill,
  isSkillAnimating,
  getSkillImage 
}) {
```

- [ ] **Step 2: 修改图片显示逻辑**

替换现有的图片渲染逻辑：
```javascript
// 获取当前显示的图片
let displayImage = current.forms && current.forms.length > 0 && current.forms[activeForm]
  ? current.forms[activeForm].image
  : current.image

// 技能动画时显示技能图片
let isAnimating = isSkillAnimating
let skillImageSrc = null
if (activeSkill && getSkillImage) {
  skillImageSrc = getSkillImage(current.name, activeSkill)
}

return (
  <div className="page-left">
    {/* 技能动画图片（淡入淡出） */}
    {isAnimating && skillImageSrc && !imageError[current.id - 1] ? (
      <img 
        src={skillImageSrc} 
        alt={`${current.name} - ${activeSkill}`}
        className="ultraman-image skill-animation"
        onError={() => onImageError(current.id - 1)}
        draggable="false"
        onDragStart={(e) => e.preventDefault()}
      />
    ) : (
      /* 原图 */
      displayImage && !imageError[current.id - 1] ? (
        <img 
          src={displayImage} 
          alt={current.name}
          className="ultraman-image"
          onError={() => onImageError(current.id - 1)}
          draggable="false"
          onDragStart={(e) => e.preventDefault()}
        />
      ) : null
    )}
    {/* 占位符 */}
    <div className="ultraman-placeholder" style={{
      background: `linear-gradient(135deg, ${current.color} 0%, #ffffff 50%, ${current.color} 100%)`,
      display: !displayImage || imageError[current.id - 1] ? 'flex' : 'none'
    }} draggable="false" onDragStart={(e) => e.preventDefault()}>
      <span className="placeholder-text">{current.name.charAt(0)}</span>
    </div>
  </div>
)
```

- [ ] **Step 3: 运行诊断检查**

Run: `lsp_diagnostics` on `ultraman-magic-book/src/components/PageLeft.jsx`
Expected: No errors

---

## Task 3: 修改 App.jsx 传递新 props

**Files:**
- Modify: `ultraman-magic-book/src/App.jsx`

- [ ] **Step 1: 解构新增的返回值**

在 `useMagicBook()` 解构中添加：
```javascript
const {
  // ... existing destructured
  activeSkill,
  isSkillAnimating,
  getSkillImage,
} = useMagicBook()
```

- [ ] **Step 2: 传递给 PageLeft 组件**

在 `<PageLeft>` 组件中添加：
```javascript
<PageLeft 
  current={current} 
  imageError={imageLoadError}
  onImageError={handleImageError}
  activeForm={activeForm}
  activeSkill={activeSkill}
  isSkillAnimating={isSkillAnimating}
  getSkillImage={getSkillImage}
/>
```

- [ ] **Step 3: 运行诊断检查**

Run: `lsp_diagnostics` on `ultraman-magic-book/src/App.jsx`
Expected: No errors

---

## Task 4: 添加技能动画 CSS 样式

**Files:**
- Modify: `ultraman-magic-book/src/index.css`

- [ ] **Step 1: 添加技能动画样式**

在 CSS 文件末尾添加：
```css
/* 技能动画淡入淡出效果 */
.ultraman-image.skill-animation {
  animation: fadeIn 0.3s ease-in-out;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* 可选：添加技能释放时���光效 */
.ultraman-image.skill-animation::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: radial-gradient(circle, transparent 30%, currentColor 100%);
  pointer-events: none;
}
```

---

## Task 5: 测试验证

**Files:**
- 测试: 启动开发服务器并验证

- [ ] **Step 1: 启动开发服务器**

```bash
cd ultraman-magic-book && npm run dev
```

- [ ] **Step 2: 手动测试**

1. 访问 http://localhost:5173/
2. 浏览到迪迦奥特曼（id=11）
3. 点击技能按钮（哉佩利敖光线）
4. 验证：图片区域显示技能图片（淡入效果）
5. 等待3秒
6. 验证：图片恢复为原图

- [ ] **Step 3: 测试其他技能**

依次测试：
- 迪迦拉休尔光流
- 兰帕尔特光弹
- 迪迦计时器闪光

---

## Task 6: 创建技能图片目录

**说明:** 技能图片需要放入 `public/images/skills/` 目录。当前任务仅创建目录占位，实际图片由你提供或AI生成。

**Files:**
- Create: `ultraman-magic-book/public/images/skills/`

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p ultraman-magic-book/public/images/skills
```

- [ ] **Step 2: 验证目录存在**

```bash
ls -la ultraman-magic-book/public/images/skills
```

---

## 验收检查清单

- [ ] Task 1-3 所有 LSP 诊断无错误
- [ ] 开发服务器正常启动
- [ ] 点击技能按钮触发动画
- [ ] 动画结束后恢复原图
- [ ] 翻页时动画取消
- [ ] 切换形态时动画取消
- [ ] 技能图片不存在时不报错（回退原图）

---

## 后续扩展

当迪迦的4个技能图片验证通过后，可扩展到其他奥特曼：
- 赛文、杰克、艾斯等昭和奥
- 戴拿、盖亚等平成奥
- 新生代系列

每添加一个奥特曼，只需在 `public/images/skills/` 目录添加对应命名的图片文件即可。